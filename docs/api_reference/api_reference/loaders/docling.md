=== "pip"

    ```bash
    $ pip install donkey-loaders-docling
    ```

=== "uv"

    ```bash
    $ uv add donkey-loaders-docling
    ```

::: donkey.loaders.docling
    options:
        inherited_members: true
        filters:
        - "!^_"
        - "!class_name"
