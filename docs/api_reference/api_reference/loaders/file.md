=== "pip"

    ```bash
    $ pip install donkey-loaders-file
    ```

=== "uv"

    ```bash
    $ uv add donkey-loaders-file
    ```

::: donkey.loaders.file
    options:
        inherited_members: true
        filters:
        - "!^_"
        - "!class_name"
