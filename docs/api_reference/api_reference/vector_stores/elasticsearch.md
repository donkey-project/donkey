=== "pip"

    ```bash
    $ pip install donkey-vector-stores-elasticsearch
    ```

=== "uv"

    ```bash
    $ uv add donkey-vector-stores-elasticsearch
    ```

::: donkey.vector_stores.elasticsearch
    options:
        inherited_members: true
        filters:
        - "!^_"
        - "!class_name"
